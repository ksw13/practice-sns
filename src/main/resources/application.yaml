
spring:
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  datasource:
    hikari:
      maximum-pool-size: 4
    url:
    username:
    password:
    platform: postgres
    driver-class-name: org.postgresql.Driver